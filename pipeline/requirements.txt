# Drevi Photography Pipeline — Python dependencies
#
# Install with:
#   python3 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt

# Google APIs
gspread>=6.0.0
google-auth>=2.30.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.130.0

# Image processing
opencv-python>=4.10.0
numpy>=1.26.0
Pillow>=10.4.0
pillow-heif>=0.18.0   # HEIC/HEIF support — iPhone default since iOS 11

# HTTP
requests>=2.32.0

# Anthropic Claude API
anthropic>=0.40.0
