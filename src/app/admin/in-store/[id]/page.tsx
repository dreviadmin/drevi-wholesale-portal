// The in-store wizard is the exhibition wizard — same flow, separate section.
// The wizard itself adapts labels and the end-session redirect by session type.
export { default, dynamic } from "../../exhibition/[id]/page";
