// Ansh (31 Jul): everything is women's garments right now, so 6204 is the
// house default HSN. Override per product (Manage Catalog / master editor /
// goods-in) whenever a different classification applies.
export const DEFAULT_HSN = "6204";
